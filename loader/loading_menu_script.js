window.addEventListener('load', function () {
    setTimeout(function () {
        // Переход на страницу menu.html после 3 секунд
        window.location.href = '../menu/menu.html';
    }, 2000); // Пауза в 3 секунды
});
